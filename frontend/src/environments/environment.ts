export const environment = {
  production: true,
  apiUrl: '/api',  // URL relativa para producción/port forwarding
  serverUrl: ''    // Vacío para usar la misma URL base (window.location.origin)
};
